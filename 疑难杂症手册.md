# 🍊 Ring_Our_Luv 疑难杂症手册

> 专治反复踩、每次都忘、换个Claude又得重查的祖传老坑。
> 按症状找，照着抄指令/checklist 就行。——小灰 & 小克 合著

---

## 一、🧊 鬼打墙：代码改了死活不生效 / 永远显示旧版

### 症状
- 本地文件明明改好了、也 push 部署了，ST 里跑的还是旧代码。
- 版本号、行为、面板统统停留在某个上古版本。
- **硬刷新、清浏览器缓存、Disable cache、无痕窗口……全都救不动**（最迷惑的一点）。

### 真因（2026-06 实锤）
ST 走 **HTTP 明文**，传输路上有一层**服务器端「透明缓存」**（云服务商边缘 / 或运营商ISP那一层），它：
- 专挑 `.js` / `.css` 这类静态文件缓存；
- **按「完整URL」当缓存key**——固定URL `index.js` 被它锁住喂旧的；
- **连 `no-store`、无痕都绕不过它**（因为它在服务器那一端，不是浏览器缓存）；
- 不加任何标识头（`via`/`age`/`x-cache` 全 null），所以特别难发现。

> ⚠️ 主窗口还会**额外多一层浏览器自己的缓存**（把旧 `manifest.json` 存住）。所以可能出现"无痕活了、主窗口还旧"——主窗口按 `Ctrl+Shift+Del` 清一次「缓存的图片和文件」即可。

### 🔬 一招诊断（控制台跑，对比固定URL vs 随机URL）
```js
Promise.all([
 fetch('scripts/extensions/third-party/Ring_Our_Luv/index.js',{cache:'no-store'}).then(r=>r.text()),
 fetch('scripts/extensions/third-party/Ring_Our_Luv/index.js?z='+Math.random(),{cache:'no-store'}).then(r=>r.text())
]).then(([a,b])=>console.log('固定URL=',a.split('\n').length,'行 | 随机URL=',b.split('\n').length,'行'));
```
- **两个数不一样** → 实锤是这层缓存在作怪（随机参数=新key=绕过去摸到真磁盘文件）。
- 两个数一样 → 不是这个坑，去查别的（比如分支没对齐、没真部署上）。

### ✅ 解法：给文件改个新名字（让缓存查无此条）
缓存认完整URL，那就换个它没见过的文件名：
1. 把入口文件复制改名，带版本号，例：`index.js` → `index-0610b.js`（**保留原文件**别删，留底）。
2. 改 `manifest.json` 的 `"js"` 字段指过去：`"js": "index-0610b.js"`。
3. `css` 同理（css 也会被缓存！）：`style.css` → `style-0610b.css`，改 `manifest.json` 的 `"css"`。
4. 部署 → ST 去请求全新URL → 缓存里没有 → 直接吃磁盘新鲜文件。🎉

> 📌 `manifest.json`（.json）**不会被这层缓存碰**，所以改名能立刻生效——这是整套解法成立的关键。
> 📌 **副作用**：以后再改 `index-0610b.js` 的内容，这个新名字又可能被锁。届时再 +1 改名（`-0610c`…），或上下面的「一劳永逸」方案。

### 🛡️ 一劳永逸（可选，懒得每次改名时再上）
- **给 ST 上 HTTPS**：加密后中间缓存就摸不到文件了，根治。
- 或做个**自动绕缓存 loader**：让 `index.js` 只当个壳，里面 `import('./core.js?v='+Date.now())`，每次加载都带随机参数，core 永不被缓存。（想做喊小克）

---

## 二、🎈 面板乱飞 / 裸奔 / 叠成一坨 / 点了没反应

### 症状
- 新加了个面板，结果它（或别的面板）飘到奇怪位置、半透明、跟背景/聊天叠在一起。
- 子面板点开没反应、缩成一团透明、布局像崩了。

### 真因
浮动面板如果**留在 `.extension_settings` 等父容器里**，会被：
- 父级的 `transform` 拽着一起飘移（CSS 里有 transform 的祖先会变成定位基准）；
- ST 新版的 **MovingUI** 在某些时机往面板**内联强塞 `transform`/`opacity`(!important)**，或直接把节点从 DOM 摘走 → 面板崩坏。

### ✅ 解法（代码里已实现，别破坏它）
在 `jQuery(async …)` 初始化里：
1. **所有浮动面板都 `document.body.appendChild` 挂到 body 顶层**，躲开父级 transform 的飘移诅咒 —— 见 [index.js:4187-4209](index.js#L4187-L4209)。
2. **自愈守卫 `MutationObserver`** 盯住 body 和每个浮层：被摘走就拎回来、被偷塞 transform/opacity 就当场撕掉 —— 见 [index.js:4211-4240](index.js#L4211-L4240)（日志带 `🛟`）。

### 🧩 加新面板时的 Checklist（照做就不飞）
1. **HTML**：在 `index.html` 里把新面板写成**独立顶层块**（别嵌在别的面板/容器里）。
2. **挂 body**：在 init 里 `const x = temp.querySelector('#新面板id'); if (x) document.body.appendChild(x);`（跟 [index.js:4188-4209](index.js#L4188-L4209) 一排并列加）。
3. **进守卫**：把新面板加进 `rolBodyPanels` 数组（[index.js:4216](index.js#L4216)），让自愈守卫也罩住它。
4. **CSS**：面板默认 `display:none` + 定位用 `position:fixed`，激活靠加 class（如 `.rol-active`），别用会被父级影响的相对定位。

---

## 三、🚀 部署流程备忘
ST 跑在云服务器、拉 GitHub **kairin** 分支。改完代码必须：
1. commit + **push 到 kairin 分支**（服务器拉的是这个分支，别只 push 到 changes）。
2. 服务器 **ST更新**（pull 最新文件）。
3. 浏览器**硬刷新**；遇上「鬼打墙」翻第一章。

---
*最后更新：2026-06-17 —— 那个折腾了好几晚的缓存悬案，今天破了。🩷*
